{
  "name": "scc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "test": "bun test",
    "e2e": "bun run src/e2e.ts",
    "gta3": "bun run cli GTA_III_SCRIPT-master -o dist --config gta3",
    "vc": "bun run cli GTA_VC_SCRIPT-master -o dist --config vc",
    "postdev": "prettier dist --write --ignore-path .prettierignore"
  },
  "devDependencies": {
    "prettier": "3.8.3",
    "@types/bun": "latest"
  }
}
